# ALX Listing App

This project is a simplified Airbnb clone built using Next.js, TypeScript, and Tailwind CSS.

## 🏗️ Project Structure

- **components/common/** — contains reusable UI components like `Button` and `Card`.
- **interfaces/** — holds TypeScript interface definitions for props and data models.
- **constants/** — stores reusable strings, API URLs, and configuration constants.
- **public/assets/** — contains images and static assets.

## ⚙️ How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/alx-listing-app.git
   cd alx-listing-app
